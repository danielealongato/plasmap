const publicPath = process.env.PUBLIC_PATH;

const paths = {
  default: `${publicPath}`,
  home: `${publicPath}home`,
  howItWorks: `${publicPath}how-it-works`,
  login: `${publicPath}login`,
  history: `${publicPath}history`,
  logged: `${publicPath}logged`,
  mapping: `${publicPath}mapping`,
  whereToDonate: `${publicPath}where-to-donate`,
  notFound: `${publicPath}not-found`,
  unauthorizedPage: `${publicPath}unauthorized`,
};

export default paths;
